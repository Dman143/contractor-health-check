import assert from 'node:assert/strict'; import test from 'node:test'; import { readFile } from 'node:fs/promises';
test('free V2 surfaces contain no benchmarks, action plans, pricing prescriptions or confirmed root causes',async()=>{const files=await Promise.all(['src/App.tsx','src/pdfReport.ts','server/index.mjs'].map(f=>readFile(f,'utf8'))); const text=files[0]+'\n'+files[1]+'\n'+files[2].slice(files[2].indexOf('const formatReportEmail'),files[2].indexOf('const logEmailRoute'));assert.doesNotMatch(text,/industry average|quick wins|30-day action plan|implementation roadmap|raise your prices/i);assert.match(text,/quick check/i)});
