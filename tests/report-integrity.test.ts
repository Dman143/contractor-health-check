import assert from 'node:assert/strict'; import {readFile} from 'node:fs/promises'; import test from 'node:test';
test('contractor-facing V2 surfaces expose no internal scores, benchmarks, diagnosis or plans',async()=>{const [app,server]=await Promise.all(['src/App.tsx','server/index.mjs'].map(f=>readFile(f,'utf8'))); const receipt=server.slice(server.indexOf('const formatContractorReceipt'),server.indexOf('const logEmailRoute'));assert.doesNotMatch(app+'\n'+receipt,/\/100|internal score|industry average|peer benchmark|quick wins|30-day|action.plan|diagnos|strongest|weakest/i);assert.match(app,/Daniel will personally review your answers/);assert.match(receipt,/TradeBuilt Quick Guide/)});
